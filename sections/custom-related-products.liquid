------------------------------------------
{{ recommendations.products_count }}
{% if recommendations.products_count > 0 %}
  {% for product in recommendations.products %}
    <div class="custom-recomend-title">{{ product.title | link_to: product.url }}</div>
  {% endfor %}
{% endif %}

{% schema %}
{
  "name": "custom-related-products",
  "settings": [],
   "presets": [
    {
      "name": "custom-related-products"
    }
  ]
}
{% endschema %}
