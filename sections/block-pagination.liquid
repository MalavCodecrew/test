{% for block in section.blocks %}
{% endfor %}

{% schema %}
  {
    "name": "block-pagination",
    "settings": [],
     "blocks": [
    {"type": "text",
     "name": "block-page",
      "settings": [{
          "type": "text",
          "id": "title_block",
          "label": "Title"
        },{
          "type": "text",
          "id": "desc_block",
          "label": "Description"
        }]
    }],
     "presets": [
  {
    "name": "block-pagination","blocks": [
      {
         "type":"text"
      }
    ]
  }
]
  }
{% endschema %}
